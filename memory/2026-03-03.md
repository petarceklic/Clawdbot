# 2026-03-03

## Possum AU Status

### Closed positions today
- **BHP.AX** — closed, entry $63.545, exited ~$59.22 (below SL of $60.28, gap/slippage). No P&L recorded.
- **WDS.AX** — closed, entry $31.905, exited ~$30.24. Time exit (SL was $27.23, not hit). No P&L recorded.
- **FMG.AX** — also shows closed in DB.

### Open positions (as of ~1:30 PM)
- **MIN.AX** — entry $62.89, 58 shares, SL $55.82, TP $68.72. At ~$55.28 (BELOW stop). Likely stopped out by IB but bot doesn't know until next morning cycle.
- **NST.AX** — entry $32.64, 114 shares, SL $28.23, TP $33.36. At $30.71 — still OK.

### Known issues with Possum AU
1. **Fill reconciliation not built** — bot places bracket orders then disconnects. Never checks fills. P&L always $0.
2. **Single pre-market cycle** — runs once ~7 AM AWST, no intraday monitoring.
3. **Today's signalled trade (SCG.AX buy)** — logged as signalled but no entry in trades table. May have been blocked by portfolio guard or IB error.
4. **Stale prices** — DB shows 7 AM prices all day. IB is executing stops but bot is unaware until next cycle.

## Anthropic API/Claude Status
- Active incident today: "Elevated errors in claude.ai, cowork, platform, claude code" — unresolved as of 1:30 PM.
- I am running on `anthropic:claude-cli` profile (OAuth), NOT api_key mode → uses Petar's Claude subscription, no extra API fees.

## Technical Notes
- Confirmed: Clawdbot uses `anthropic:claude-cli` OAuth profile (Claude Max/Pro subscription covers it)
- Config has an `anthropic:default` api_key profile but it's NOT used for main session
